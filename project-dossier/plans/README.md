# Adoption plans

The authoritative future adoption plan is
[`machine-readable/plan.json`](../machine-readable/plan.json). It describes
dependencies and acceptance evidence only. It is not a task ledger, lease,
receipt, assignment, or permission source.
