# Dossier version

Version: `1.3.0-mapped-existing`

This dossier metadata revision adds explicit compatibility dispositions, a local
read-only planner interpretation route, precise external Blueprint maintenance
requirements, and the dated full-archive forward baseline. Crosswalk v2 renames
the current maintenance group while retaining its stable IDs and exact v1
predecessor in Git. It preserves the completed `external_state` epoch, clean
ordinary entry, retained migration procedure, origin-v3 correction and scoped
editor amendment. It does not reversion either 1.1.0 handoff package or adopt a
new Blueprint.

Selected structural reference: installed Project Blueprint 1.0.0, high-assurance,
mapped-existing. Clean committed Octon Mini 4.2.0 is identified but fails full
qualification; dirty working VERSION 4.3.0 is uncommitted and separate.
No upgrade is applied. See [provenance](provenance/README.md).
