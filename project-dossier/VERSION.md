# Dossier contract version

Candidate contract version: `1.4.1-mapped-existing`.

This compatible revision adds scoped standalone operating references, explicit
code/control validation, navigable owner sets, complete remediation dispositions
and dependency-based workspace retention. The current `external_state` epoch,
85 mappings, ten compatibility dispositions and all prior completion evidence
remain. It does not version either sealed/source package or assert candidate
approval. See [supersession](SUPERSESSION.json) and
[ADR-0007](../docs/decisions/ADR-0007-standalone-workspace-operating-contract.md).

The correction makes facade self-commands scope-specific, replaces duplicate
deferral details with closed RAIDQ references, and pins all 85 mapped
dispositions to immutable accepted history. The schema-v1 predecessor at
`a0bdca755d1df6f7c1cf791d9d1f7641c2baa107` remains a rejected candidate in Git,
not an accepted or integrated dossier contract. This schema-v2 correction
requires independent candidate acceptance before activation.
