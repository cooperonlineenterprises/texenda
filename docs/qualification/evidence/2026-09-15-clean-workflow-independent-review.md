# Independent review: clean ordinary Texenda workflow

Verdict: **APPROVE** with zero unresolved candidate findings for
`48033f36b448167511bddd379fde04684918fdb1`, tree
`0ab6e4014bf43a3378874c4ed70b2112793e2a64`.

This approval covers the exact ordinary-entry, current-status, transition-history,
and validation candidate. It also records a delayed present audit of the inherited
editor/Blueprint follow-up. It does not approve a containing evidence commit,
future refresh or integrated head, Codex registration, later compatibility work,
product readiness, a Blueprint upgrade, or an external effect.

Preserve this evidence, refresh the declared generated views, independently audit
the exact resulting head, then integrate locally only under the governing owner
authority. That future audit must be separate from this candidate approval.

## Exact subject, independence, and bounds

| Field | Value |
|---|---|
| Base | `338cd13505d6faffdb645c450b44ea9230aadd75` |
| Base tree | `fcb8ec5b252663fea73d188e18768c54d544399a` |
| Candidate | `48033f36b448167511bddd379fde04684918fdb1` |
| Candidate tree | `0ab6e4014bf43a3378874c4ed70b2112793e2a64` |
| Canonical full-index binary diff SHA-256 | `65650d9a011401084da7d801427010d55532a88c7e6ef121c7707286a0e81f04` |
| Candidate author | `/root/clean_workflow_author` |
| Independent reviewer | `/root/clean_setup_reviewer` |
| Integrator | `/root` |
| Review worktree | `/Users/jamesryancooper/Projects/texenda/worktrees/clean-workflow-review-20260915` |
| Review completed observation | `2026-09-15T16:30:02Z` |
| Evidence-authoring entry | `2026-09-15T16:31:49Z` |

The parent explicitly assigned T1 `gpt-6-astra/max`, exact profile
`gpt-6-astra-max`, runtime `codex-desktop-collaboration`, subscription billing.
Review bounds were 80,000 tokens and 3,600 seconds. This later evidence-only
follow-up has separate bounds of 20,000 tokens and 1,200 seconds. API allowance
and spending are USD 0. No fallback, downshift, child agent, or live-ledger
assignment was used. Provider executing-model introspection and measured token
accounting are unavailable; the reviewed live qualification is not independent
provider telemetry.

The exact live qualification was rechecked before evidence authoring. It identifies
client `26.901.41600 (build 7982)`, read/edit/test capabilities, verification time
`2026-09-13T21:44:31Z`, expiry `2026-10-12T21:44:31Z`, and required check
`desktop-profile-gpt-6-astra-max`. Its owner-attested source is
`docs/qualification/model-roster-v2.1.evidence.json`, SHA-256
`8e44034e6247ea47b3f0002100e6a343cfa6685d56138a24f55e8a13054173c0`.
The live harness reported that exact profile available. This paragraph is a
dated qualification observation, not a second roster or fresh qualification.

Routing-policy digest:
`61f0db448cf5ac5adad4d9340d568a7558c0369174e30aab92dd0ccf0d24bdf0`.
Work-package digest:
`9ad162070c27e4c765ba145fc8a35d31dc288482453daaa39ec0bb6add8bd8d8`.

Canonical diff serialization:

```text
git --no-optional-locks -c core.abbrev=40 -c color.ui=false diff --no-ext-diff --no-textconv --full-index --binary 338cd13505d6faffdb645c450b44ea9230aadd75 48033f36b448167511bddd379fde04684918fdb1 | shasum -a 256
```

An initial ordinary `git diff --binary` produced
`58efc1eda6f0179041470a0c30f7a0ee9bc9649b40c105c250857c8d18248f43`.
The difference is abbreviated versus full index lines. The canonical serialization
above matches the assignment; no candidate correction was needed.

## Substantive review

The reviewer read the applicable root and nested instructions, facade kernel,
accepted ADRs, dossier authority map, current crosswalk, relevant routing contract,
operating guide and assignment/review/resumption templates, sealed orientation,
kernel and invariants. The installed project-bootstrap 1.0.0 skill and relevant
dossier/harness/high-assurance/adoption references supplied structural guidance
within Texenda's mapped authority. No stock scaffold, new Blueprint version, or
other project's facts were adopted.

The complete 38-file candidate diff was inspected, including the executable
refresh/validation changes and tests. The generated files were validated through
deterministic reconstruction of all eleven outputs. The history addition was
compared to its exact prior Git object and checksum.

The candidate provides one explicit ordinary validation command and bound
`status`, `ready`, and `context WP-01` examples. Initialization and legacy-ledger
migration appear in maintenance guidance after ordinary entry. No wrapper,
automatic initialization, state discovery, or hidden fallback was introduced.
The existing templates still require exact scope, profile, evidence, independent
review, remaining bounds, and runtime-stop/recovery proof.

ADR-0006 supersedes only stale present-tense interpretations of previously
integrated records. It does not amend routing, permissions, sealed semantics,
editor scope, recovery, qualification, or external gates. ADR-0001, ADR-0004,
ADR-0005 and existing review evidence retain their exact bytes. The current
crosswalk identifies `completed_current_contract` and `external_state`; v3 is
the active origin adapter and v2 remains hash-bound predecessor history.

Editor resolution remains limited to ADR-0005's reversible composer/codec
direction. Exact version remains null/UNVERIFIED. WP-10 and VAL-03 still require
their evidence. Both package variants retain their original roles and all eleven
differences. No package is promoted or merged.

The exact 219-line former transition README is retained at
`project-dossier/history/2026-09-15-workspace-transition-completed.md`, SHA-256
`1f5b0b1aa816f6bcd34f591a0b5f9f5b4e9d6e8bf35474ca77367418fb930919`.
The catalog and supersession record bind its former path, source revision, exact
hash, retained path, and active successor. Its original relative links are
deliberately excluded from current-link checking while the entire snapshot is
hash-validated; `history/README.md` supplies current navigation. The concise
transition entry retains access to operational recovery guidance.

Generated current-state, source-map and handoff views remain explanatory and
source-bound. They lead ordinary work to current owners and the active ledger.
The `ready` command is a live query; no second ready-work list, task ledger,
receipt chain, model roster, routing policy, decision store, evidence store, or
permission owner was introduced. The owner-map checks cover 42 concerns.

## Exact validation and results

Unless a different working directory is stated, commands ran from the detached
review worktree with `PYTHONDONTWRITEBYTECODE=1` and `GIT_OPTIONAL_LOCKS=0`.

The exact documented ordinary command was run as written:

```text
env PYTHONDONTWRITEBYTECODE=1 python3 -B .agent/scripts/validate.py --check --all --state-root /Users/jamesryancooper/Projects/texenda/local/agent-state/texenda
```

It passed in 17.771 seconds. All nine dispatched registry commands exited 0;
both refresh writers were skipped. The dispatched commands were:

```text
python3 -B tooling/workspace/validate_contract.py --check --audit
python3 -B -m unittest discover -s .agent/tests -p 'test_*.py' -v
python3 -B -m unittest discover -s tooling/workspace/tests -p 'test_*.py' -v
python3 -B -m unittest discover -s tooling/coordination/tests -p 'test_*.py' -v
python3 -B -m unittest discover -s specs/texenda-handoff/08-project-harness/tests -p 'test_*.py' -v
python3 -B specs/texenda-handoff/10-validation/validate_package.py --checksums
python3 -B /Users/jamesryancooper/Projects/texenda/sources/handoff-1.1.0-20260914/10-validation/validate_package.py --checksums
python3 -B tooling/coordination/harness.py --root /Users/jamesryancooper/Projects/texenda/worktrees/clean-workflow-review-20260915 --state-root /Users/jamesryancooper/Projects/texenda/local/agent-state/texenda check
git diff --check
```

Test discovery confirmed 90 workspace, 187 coordinator and 39 sealed tests; all
passed in the registered run. Both package validators passed 14/14 checks. The
standalone facade suite was also run with the exact command above: 34 tests passed
in 49.571 seconds without skips. This includes the complete-check fixture skipped
during recursive `--all` execution, so its omission was not hidden.

The workspace contract also passed independently: 42 concerns, 85 Blueprint
paths, 35 artifact types, 16 retained moves, 86 sealed files and 88 source files.
Facade validation parsed 133 JSON, one TOML and 19 Python files; validated 30
dossier paths, three instruction files, 126 local links and 473 hash-bound
PASS/FAIL checks. All eleven generated outputs reconstructed exactly; source
scope was `36d57188cfcf38cd8de13bff24f5e6d28d284157ce12d826664fc662ae39fb70`
and generation was `8fca60b8a528b424656b61fa197e3231c2766c475e70ad6e8fa66d45f47ed621`.

The following commands each exited 0:

```text
env PYTHONDONTWRITEBYTECODE=1 python3 -B tooling/coordination/harness.py --root . --state-root /Users/jamesryancooper/Projects/texenda/local/agent-state/texenda status
env PYTHONDONTWRITEBYTECODE=1 python3 -B tooling/coordination/harness.py --root . --state-root /Users/jamesryancooper/Projects/texenda/local/agent-state/texenda ready
env PYTHONDONTWRITEBYTECODE=1 python3 -B tooling/coordination/harness.py --root . --state-root /Users/jamesryancooper/Projects/texenda/local/agent-state/texenda context WP-01
```

Ready work was `WP-01`, `WP-18`; WP-01 context was READY and retained T1/max
author/reviewer floors, governing owners and the local policy digest. The command
prepares context only; it does not admit or assign work. Assignment, review,
handoff and resumption templates were inspected, and their instruction-contract
tests passed. No live lifecycle mutation was performed.

From canonical `repo/`, omitting the state root was independently denied:

```text
python3 -B .agent/scripts/validate.py --check
python3 -B tooling/coordination/harness.py --root . status
```

Both exited 2 with respectively `active binding requires explicit --state-root`
and `bound projects require explicit --state-root`. State bytes were unchanged.
The registered mutation/recovery suites also exercise wrong/missing roots,
competing stores, symlink/path escape, receipt corruption, locks, stale generated
views, private-path exclusions, and interrupted recovery.

Complete candidate and inherited ranges passed `git diff --check`. Exact protected
comparisons found no candidate changes to `specs/texenda-handoff`, ADR-0001/4/5,
existing qualification evidence, `tooling/coordination/harness.py`, routing policy,
`.agent/policy.json`, `.agent/context.json`, or `.project-blueprint-origin.json`.

The installed planner and generator were run read-only:

```text
python3 -B /Users/jamesryancooper/.codex/skills/project-bootstrap/scripts/plan_adoption.py --target /Users/jamesryancooper/Projects/texenda/worktrees/clean-workflow-review-20260915 --profile high-assurance
python3 -B /Users/jamesryancooper/.codex/skills/project-bootstrap/scripts/scaffold_project.py --target /private/tmp/texenda-clean-review-dryrun-20260915 --project-name Texenda --profile high-assurance --dry-run
```

Both exited 0. The planner found 45 literal collisions and 40 candidate paths;
all 85 have dispositions in the mapped crosswalk. Generic origin inspection
still reports `valid_shape_not_fully_validated` and no generic version value;
the project-local v3 validator provides the exact scoped validation. The dry run
reported 85 intended paths; its target was absent before and after. No scaffold
was written.

## Read-only and preservation proof

An in-memory before/after snapshot surrounded the exact candidate `--check --all`
run. It covered all 360 paths in the review worktree and external state directory,
including directories, regular-file hashes, symlink targets if present, mode,
size and nanosecond atime/mtime/ctime. The comparison reported an empty change set.
This includes tracked, untracked, ignored, generated, cache and lock paths inside
those measured roots. Git object-store internals beyond the worktree's `.git`
pointer, other worktrees, private inputs and disposable fixtures outside the roots
were not part of that snapshot. No private path was present in its traversal.

Candidate worktree remained clean at the exact candidate/tree. Canonical main
remained clean at `338cd13505d6faffdb645c450b44ea9230aadd75` before and after its
separate audit. The later two authorized evidence additions are outside that
completed read-only interval.

| Preserved item | Result |
|---|---|
| External `state.json` SHA-256 | `b993eebdba5d2d8e5597c7205ee58846bf83ecb9d553bc55a687dcf1f7880235` |
| State version | `2.0` |
| Receipt count | 13 |
| Receipt tip | `f39249184757101b108ea7ec5ea0ffebdd8237315b8ac3cad3ad0cc6ab3e827e` |
| Live profiles | 11, exact reviewer profile available |
| Leases | 0 |
| Declared budget/spend/remaining | USD 0 / 0 / 0; no budget approval |
| Cleared gates | None; gate map empty |
| Sealed `SHA256SUMS` | `37fffb1a165cfa6b907e7433d617c06e1c64ff09cd282173f7d791cace2ac07e` |
| Source `SHA256SUMS` | `8576bf08ec4f2a4718476eca70e6d1d5cb2b6be1af550bf5c20c692bcbaff715` |

The source and sealed package validators plus byte comparisons against the
recorded baseline preserve both variants and their eleven recorded differences.
WP-10 remains planned and VAL-03 remains unpassed. The external ledger was read
only; no receipt, lease, budget, roster or gate changed.

Private handling used only exact `test -f` for
`/Users/jamesryancooper/Projects/texenda/local/private-inputs/kit/ohw/2026-09-14/subscribers.csv`
and Git ignore/tracking queries. The exact final file exists; the legacy relative
path remains ignored and no private CSV is tracked. No private content, size,
hash, copy, parse, or directory listing was accessed. Existence and tracking do
not establish private-content equality, and none is claimed.

## Delayed audit of the inherited editor/Blueprint follow-up

Inherited range:
`836e209020177267ccaa1c803b66779cd29d12ec..338cd13505d6faffdb645c450b44ea9230aadd75`.
Its canonical full-index binary diff SHA-256 is
`40f7cde73d8c79490b5d3b4ab53b6a6dbd3a0ec063c9f0c30f5d17d1b21e8b59`.

The inherited editor/Blueprint result at `338cd13505d6faffdb645c450b44ea9230aadd75`,
tree `fcb8ec5b252663fea73d188e18768c54d544399a`, passes this delayed present audit.
The reviewer inspected its implementation/schema/test and provenance changes,
the preserved independent review, and the exact two subsequent commits.
From previously approved `36e17d84ce28d80550867bb3d33500f4db5b1ca2`, only two
review files and eleven generated views changed. Executable and authoritative
source bytes are identical. Existing review/author/observation hashes validate.

The exact canonical `repo/` `--check --all` command passed at unchanged head
`338cd13505d6faffdb645c450b44ea9230aadd75`; all nine dispatched commands exited 0.
Its 28 dossier paths, 111 links, 473 PASS/FAIL evidence checks and deterministic
generated reconstruction passed. Its source scope remains
`ed42a7f8fc518e5a111d82c10adc84278b8f0b4ff7d942bf1e54c5e48cb8ea0d`.
The canonical worktree remained clean and at the same head/tree after checking.

The historical review required an audit of the evidence-bearing head before
integration, but the retained record does not establish that this happened then.
The old head was already integrated before this new audit. **This is a recorded
timing exception.** This dated successor closes the missing present audit; it
does not retroactively prove timely compliance or rewrite the old NOT_RUN rows.
The original review and its activation condition remain unchanged.

Selected installed Blueprint 1.0.0 remains structural reference only. Clean
committed 4.2.0 at `5e2d3025aea6b1574ab984e5ebb89b5602a38535` remains unqualified
with PASS/PASS/FAIL qualification, distinct from the dirty uncommitted 4.3.0
observation. No reviewed migration seed exists and no upgrade is approved.
The earlier independent full-validator failure evidence was inspected and its
failure remains enforced. The upstream suite was not rerun or repaired in this
candidate review; no fresh source qualification is claimed.

The retained Blueprint bundle was independently hashed and verified:

```text
git --no-optional-locks -C /Users/jamesryancooper/Projects/texenda/repo bundle verify /Users/jamesryancooper/Projects/texenda/archive/checkpoints/blueprint-qualification-20260915/octon-mini-4.2.0-5e2d302.bundle
shasum -a 256 /Users/jamesryancooper/Projects/texenda/archive/checkpoints/blueprint-qualification-20260915/octon-mini-4.2.0-5e2d302.bundle
```

Both exited 0; the bundle records complete history and the exact main ref above,
SHA-256 `d8e1471a25c5d0d20bd560d0dd33f060d6d0013aa573e3f725b2ce93639b3d7e`.

From `archive/reviewer-caches/pre-fix-check-all-KHHptR`,
`shasum -a 256 -c CURRENT-SHA256SUMS` verified all five archive files.
`CURRENT-SHA256SUMS` remains
`8fca896ff26a00eb7d85d49fe54344a6a2d9b034d5938609ca920c3bc7ac2bd2`;
`PROVENANCE.md` remains
`5830d7d810d203a3e9e0f3fc9c2859778424be6d5bd4ccddcf512fc8699b0fa2`.
This is a forward checksum baseline only. Pre-migration equality cannot be
reconstructed. Raw logs and bytecode were not promoted to source or authority.

## Limits and evidence-only handoff

No candidate defect required a correction. Read-only probes that used the wrong
sealed orientation path or an abbreviated diff serialization were corrected to
the actual path and canonical flags; no missing required read or digest mismatch
was silently accepted.

No GitHub, Codex UI, global skill, upstream source, private input, package or live
state was modified. No network operation, push, deployment, publication, spending,
workflow activation, or external-gate clearance occurred during this review.
Current GitHub visibility was not independently re-queried by this reviewer;
the integrator must retain its separately observed external-state evidence.
Project-home navigation and Codex registration are outside this candidate's
reviewed Git delta.

All review command sessions completed; no child agents or background jobs remain.
The parent must observe this evidence-authoring follow-up's final stop before
releasing the worktree. This document does not claim its writer is stopped while
authoring it.

Only this Markdown and the paired closed v2 review envelope may be added in the
evidence-only child commit, whose exact parent is the approved candidate. Every
PASS envelope check references this file and its exact SHA-256. `TRN-WSM-0001`
is an existing transition reference, not a new admitted WP or receipt. The
containing commit/tree and evidence hashes are reported separately to avoid
self-reference. Adding these records makes the generated evidence index stale;
only the integrator's explicit refresh and another exact-head audit can close
that subsequent step.
