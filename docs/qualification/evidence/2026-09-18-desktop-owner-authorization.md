# Owner authorization record — current desktop qualification

Recorded from the current owner message on 2026-09-18, titled
“Authorization: qualify current desktop profiles and resume preparation”.
This is bounded evidence of that instruction, not another permission policy.
The current instruction itself controls; this record grants no broader power.
Actual command operator/integrator: `/root` (AI). Independent verifier/reviewer:
`/root/desktop_qualification_reviewer_20260918`. Neither is the human owner.

## Exact controlling excerpts

> I authorize bounded runtime qualification on the current Codex desktop, followed by the reviewed forward roster update needed to resume the previously authorized Texenda pre-build preparation.

> This is a narrow exception to the preparation task’s requirement that live coordination state remain unchanged. The exception covers only the active qualification roster, its supporting evidence, the normal append-only roster-update receipt, and necessary generated-view refreshes.

> It does **not** authorize changing the product plan, admitting WP-01, or beginning product implementation.

> Candidate profiles may be dispatched before their new qualification is installed **only** for:
>
> - Isolated, deterministic qualification probes.
> - Independent verification of the resulting qualification evidence.

> This limited bootstrap permission prevents qualification from requiring an already-qualified copy of the same runtime. It does not establish qualification by itself or permit ordinary preparation/product work through an unverified route.

> This message supplies my conditional owner authorization to install the successfully demonstrated, independently reviewed qualification results.

> It is not a claim that unperformed probes have passed. Retain this authorization as the approval basis, identify the actual AI operator and independent reviewer separately, and use the established owner-attested roster procedure without suggesting that the AI is the human owner.

> If the existing procedure requires additional personal verification that cannot be satisfied by this authorization and the observed evidence, report that exact remaining requirement.

## Remaining conditions retained from the same instruction

- Only necessary max-effort Astra, Sol, Terra or Luna profiles; select subset
  before dispatch including independent review. Astra/max and Terra/max were selected.
- Existing authenticated desktop subscription only. No API spending, paid credits,
  usage resets, upgrades, new account connections, lower efforts or CLI transfer.
- At most two concurrent probes/writers with disjoint scopes, one initial probe
  per required profile, at most one retry only for a clearly transient failure;
  maximum ten minutes each and one hour including qualification review/update.
  Finite token limits declared; unavailable enforcement/measurement disclosed.
- Verify actual client, permissions, Git and state. Preserve failed evidence,
  historical records and true validity windows; independent exact-candidate
  review precedes the normal coordinator update and local integration.
- Use the existing coordinator only. No manual ledger edit, initialization,
  receipt rewrite, guard modification or improvised recovery. Any affected
  assignment/lease recovery requires a stop and report, not termination of unrelated work.
- Preserve product tasks, previous receipts, effective plan, routing policy,
  budgets, gates, both sealed/source variants, research and private boundaries.
- No product implementation/admission/gate clearance, push, deployment,
  publication, provisioning, GitHub mutation or external-service connection.
- Roster-write exception ends at qualification closeout; resume the original
  preparation with WP-01 undispatched and no product work started.

## Attestation versus execution

The existing `set-roster` guard requires `--actor human:owner`. That value
attributes the above conditional attestation to the actual owner; it does not
claim that the human operated the shell or that the AI is human. The separate
actual operator is `/root`. The coordinator does not authenticate either actor.
No additional personal interactive ceremony is imposed by the existing guard;
independent review must confirm the recorded conditions are actually satisfied.
This explanation does not modify or bypass the guard.
