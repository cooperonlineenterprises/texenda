# Independent review — focused initial-product preparation

**APPROVE source** `0e4c85560d1f1074616b4ab0b6c34774f7f43529`, tree
`624039d927abbe90df6e69f4aeb8db93466169f4`, against accepted base
`79db6081dd929af3f46a1cc8e1eec5f8ac09e9cc`.
Full-index binary diff SHA-256:
`0d0e6882c448415e893b9b0e22fb56fe33a11cbc51469517530a7969a7398dcd`.
This is preparation-source approval only, not approval of the containing evidence
commit, WP-01 admission, product tests, external qualification or activation.

## Scope, actors and routes

The [maintenance record](2026-09-18-preparation-plan.md) records the owner-authorized
scope, artifact plan, preserved baseline, reuse and finite bounds. It is not a task
ledger. Four explanatory files and one navigation link instantiate existing owners:

- [Journey sketches](../../implementation/preparation/journeys.md): five families,
  shared low-fidelity screens, source revision and accessible prose equivalents.
- [Synthetic cases](../../implementation/preparation/synthetic-cases.md): twelve
  documentation-local cases and eight invented seeds; **NOT EXECUTED**.
- [Prepared assignment](../../implementation/preparation/wp01-assignment.md):
  WP-01 remains unallocated, not admitted or dispatched; fresh authorization/checks required.
- [Future Kit checklist](../../migration/kit-discovery-preparation.md): no discovery
  or source-control operation, and no new synthetic-foundation prerequisite.

Packet author: `/root/preparation_packet_author`, explicitly dispatched
`gpt-6-astra/max`. Lead/navigation/evidence author and integrator: `/root`.
Read-only inventory: `/root/desktop_terra_probe_20260918`, retained explicit
`gpt-5.6-terra/max` selection after current-build qualification.
Independent reviewer: `/root/desktop_qualification_reviewer_20260918`, explicitly
selected `gpt-6-astra/max`, distinct from both authors and integrator.

Manual Model Reasoning Router selection was used because no separate named
callable interface was available. T1 was required for permission, consequence,
recovery, shared-command interpretation and final review; T3 handled only bounded
read-only inventory. No T2/T4 dispatch, fallback, downshift, API allocation or
CLI execution qualification was inferred. Actual provider identity/effort
introspection and measured token use are **UNKNOWN**, not zero.

Current roster source: [v2.2](../model-roster-v2.2.evidence.json), SHA
`287301864a074adbf920ea83756e40adcb1368cee1a9bf8a18cbe6c221b627a6`.
Runtime: desktop collaboration26.908.70816(build9275), subscription, read/edit/test.
Astra/max expiry2026-09-25T16:04:05Z; Terra/max expiry16:03:50Z that day.
Live qualifications, hashes and actual bundle were checked before each dispatch:
inventory16:37:56Z; author16:42:01Z; author clarification17:08:22Z;
initial review17:16:04Z; S4 author repair17:25:28Z; re-review17:28:46Z.
Coding qualification is not Texenda product AI/agent/provider qualification.

Bounds: preparation hard deadline19:37:57Z, after qualification closeout;
author3600seconds/35000tokens with absolute deadline17:43Z;
inventory900/10000; source review1800/20000; corrected review600/8000.
Author clarification600/8000 and S4 repair300/3000 stayed within the original
author scope/deadline/allocation; they did not renew the owner allowance.
Provider token measurement/enforcement is unavailable. A transient usage snapshot
at16:56:46Z allowed ordinary subscription work, with weekly38% used; other windows
were unavailable. This is dated observation, not a permanent quota or token count.

## Findings, corrections and immutable candidates

Author candidate `eda661a0b48f6bb0bb8be1076b7af29bfe8d001c` was preserved.
Lead inspection requested a bounded successor `eb5eabd3e08b636e757cf1f413d0047406520ce7`:
separate product AI from coding-runtime requirements; clarify authorized canonical
generated refresh versus forbidden live-state/worktree mutation; avoid inventing
a prohibition on otherwise authorized future WP-01 schema artifacts; describe
Sol as not active-qualified rather than account-unavailable; add concrete invented
seed examples without setting production defaults or schemas.

Independent review of combined source`4d883b8f7b7f8170105ac150fbf542f76927485e`
found one P2: S4's universal arrow implied human approval for every read/draft.
That candidate was **not approved**. The author changed only S4's frame and
accessible prose at`868342aaea4ae42eb4131ab3666c72dc0c5d1dc8`, integrated as
`837433a` and refreshed in the approved source above. Reads/reversible drafts
return directly where approval is not required; exact-approval paths are
conditional, and queries acquire no new receipt/idempotency requirement.
No command or lifecycle authority changed. The reviewer found no further issue.

## Actual validation and evidence limitations

Author commands (reported with successful exits and stopped sessions):

```sh
env GIT_OPTIONAL_LOCKS=0 PYTHONDONTWRITEBYTECODE=1 python3 -B .agent/scripts/validate.py --check --scope code --all
git diff --check
git diff --cached --check
```

Full code registry passed on author candidate eda661a: ten applicable delegated
commands,152JSON,1TOML,28Python,144 registry links,606bound checks. Prose corrections
received targeted `--check --scope code`, whitespace and supplemental checks.
Worktree code PASS explicitly left live/control/generated freshness unassessed.

Integrator full canonical command on source4d883b8:

```sh
env GIT_OPTIONAL_LOCKS=0 PYTHONDONTWRITEBYTECODE=1 python3 -B .agent/scripts/validate.py --check --all --state-root /Users/jamesryancooper/Projects/texenda/local/agent-state/texenda
```

PASS: all13delegated commands exited0; both refresh writers skipped. This includes
coordinator212,workspace117,facade83(with aggregate recursion guards),sealed39,
and both14-check package validators. Parsing159JSON/1TOML/28Python,
223registry links,606bound evidence checks; live integrity and generated freshness
passed. After the S4-only prose change, targeted canonical control/freshness and
supplemental checks passed again on exact0e4c855. Full unchanged suites were not
repeated for that prose correction; integrated main receives the full registry.

Independent reviewer exact command on corrected source, PASS by17:30:25Z:

```sh
env GIT_OPTIONAL_LOCKS=0 PYTHONDONTWRITEBYTECODE=1 python3 -B .agent/scripts/validate.py --check --state-root /Users/jamesryancooper/Projects/texenda/local/agent-state/texenda && git --no-optional-locks diff --check 79db6081dd929af3f46a1cc8e1eec5f8ac09e9cc 0e4c85560d1f1074616b4ab0b6c34774f7f43529
```

The initial review used that same command with final revision4d883b8. It also
ran the read-only canonical harness status, ready and context WP-01, confirming
exact paths, prerequisite WP-00, current digests and the prepared assignment's
snapshot. Both author and reviewer independently inspected link/anchor and
source-hash bindings. Root's bounded supplemental check covered123packet/entry
links,52source hashes, valid expanded slash-group AC/WP references, five journeys,
and twelve unique NOT EXECUTED cases. Reviewer additionally counted114expanded
AC/WP references and eight seed rows. These are structural/design checks, not
execution of the product scenarios.

Two one-off checker failures were preserved in the tool observations: root's
initial Python regex was overescaped and matched no case headings; the reviewer's
initial seed-row header filter counted7rather than8. Each was corrected once and
rerun successfully without changing repository validators or product content.
No new validation framework or command-registry owner was added.

The integrator's exact supplemental inspection is reproduced below. Run it from
the canonical repo or a clean candidate root with the same environment prefix:
`env GIT_OPTIONAL_LOCKS=0 PYTHONDONTWRITEBYTECODE=1 python3 -B -`.
It performs reads only and requires the existing plan loader; it is an audit
procedure, not a shipped product test or replacement command registry.

```python
import hashlib,json,re,sys,urllib.parse
from pathlib import Path
root=Path('.').resolve()
sys.path.insert(0,str(root/'tooling/coordination'))
from plan import load_plan
plan=load_plan(root)
paths=[root/'docs/implementation/preparation'/name for name in ('journeys.md','synthetic-cases.md','wp01-assignment.md')]+[root/'docs/migration/kit-discovery-preparation.md',root/'docs/implementation/initial-product.md']
def anchors(p):
    text=p.read_text(); ids=set(re.findall(r'<a\s+id=["\']([^"\']+)["\']',text))
    counts={}
    for heading in re.findall(r'^#{1,6}\s+(.+)$',text,re.M):
        slug=re.sub(r'[^\w\- ]','',heading.strip().lower()).replace(' ','-')
        n=counts.get(slug,0);counts[slug]=n+1;ids.add(slug+(('-'+str(n)) if n else ''))
    return ids
links=0
for p in paths:
    text=p.read_text()
    for target in re.findall(r'\[[^\]]+\]\(([^)]+)\)',text):
        target=urllib.parse.unquote(target)
        if '://' in target or target.startswith('mailto:'): continue
        raw,_,fragment=target.partition('#'); dest=(p.parent/raw).resolve() if raw else p
        assert dest.is_relative_to(root) and dest.exists(),(p,target)
        assert not dest.is_symlink(),(p,target)
        if fragment and dest.suffix=='.md': assert fragment in anchors(dest),(p,target,fragment)
        links+=1
    for prefix,nums in re.findall(r'(AC-[A-Z]+|WP-)([0-9]{2}(?:/[0-9]{2})*)',text):
        for num in nums.split('/'):
            key=prefix+num
            allowed=plan.work if prefix=='WP-' else {r['id'] for r in plan.acceptance['criteria']}
            assert key in allowed,(p,key)
assignment=paths[2].read_text(); hashes=0
for rel,expected in re.findall(r'^\| `([^`]+)` \| `([0-9a-f]{64})`',assignment,re.M):
    p=root/rel
    if not p.exists():p=root/'specs/texenda-handoff'/rel
    assert p.is_file() and hashlib.sha256(p.read_bytes()).hexdigest()==expected,rel
    hashes+=1
cases=paths[1].read_text();chunks=re.split(r'^## (PREP-CASE-[0-9]{4})[^\n]*\n',cases,flags=re.M);ids=chunks[1::2];assert ids==['PREP-CASE-'+str(i).zfill(4) for i in range(1,13)]
for key,body in zip(ids,chunks[2::2]):
    for marker in ('NOT EXECUTED','Preconditions / inputs','Actor / action','Visible / persistent result','Denial / recovery'):
        assert marker in body,(key,marker)
    assert ('Verification / limit' in body or 'Automated specification / limit' in body),key
journeys=paths[0].read_text();assert all('id="j'+str(i)+'"' in journeys for i in range(1,6))
assert 'PREPARED, NOT ADMITTED OR DISPATCHED' in assignment
assert 'NOT AVAILABLE and NOT RUN' in assignment
print(json.dumps({'status':'PASS','files':len(paths),'local_links_and_anchors':links,'source_hash_bindings':hashes,'valid_explicit_and_slash_group_AC_WP_references':True,'journey_families':5,'unique_NOT_EXECUTED_cases':len(ids),'product_tests_executed':False,'effective_plan_digest':plan.digest},indent=2))
```

## Preservation and stop proof

The qualification exception had already closed at main79db608. Throughout this
preparation, the entire live state remains
`43531481205f0d410bdf12a242033823d1c66edef035e3bb0f4bd4e0d7592906`, schema2.1,
15receipts, tip`0a01ea216c946654105b4eb23a1b312ca45694ecbc27e9fecf1877cc3548605d`,
zero leases/APIbudget and unchanged gates. WP-01 remains planned, unassigned,
fence0; no admission/start/completion was recorded. Effective plan remains
`16818d77078879f8e1ede4ca5b01174b3cd3df71fcd1239bb9eea6399c6a9f7e`;
routing remains`61f0db448cf5ac5adad4d9340d568a7558c0369174e30aab92dd0ccf0d24bdf0`.

An11-file external-state-directory snapshot at16:50:46Z and comparison at17:18:40Z
matched content, path membership, mode, size, mtime and ctime. OS-managed atime
stability is not claimed. Both handoff variants/research retain their prior
per-file baselines; package validators pass. No private content,size,hash,
credentials or production data was accessed. No application process, product
schema/migration/feature, account connection, push or external effect occurred.
All product tests/real operator, model, agent and provider observations remain
NOT EXECUTED or their existing unverified gate status.

Inventory completed; packet author completed/stopped after its correction;
independent source reviewer completed with all subprocesses exited. Lead remains
active only for evidence, designated refresh, exact evidence-head review and
serial local integration. Do not release/remove the clean author worktree until
all associated runtime scopes are stopped. Keep branches, commits and review
history. Any substantive source correction requires another exact review.
